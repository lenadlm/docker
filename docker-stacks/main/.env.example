# ⚠️ Copy to .env and fill in real values. NEVER commit .env to git.
# === n8n ===
N8N_DB_PASSWORD=change_me
N8N_ENCRYPTION_KEY=change_me_32_chars_min
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=change_me

# === Postgres ===
POSTGRES_USER=n8n
POSTGRES_PASSWORD=change_me
POSTGRES_DB=n8n

# === Redis ===
REDIS_PASSWORD=change_me

# === Network ===
N8N_HOST_IP=192.168.1.220